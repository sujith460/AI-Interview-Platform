# Direct API base URL (used by axios in production builds)
# Leave empty in dev to use the Vite proxy instead
VITE_API_BASE_URL=http://localhost:8080

# Dev-only: backend target for the Vite /api proxy
VITE_API_PROXY_TARGET=http://localhost:8080
